package first;

import org.testng.annotations.Test;

public class NewTest {
  @Test  //after @ symbol is annotation ,it provide multiple execution
  //it run as independent
  //run as alphabetic order by default
  //priority will start with 0 or 1
  public void f() {
  }
}
